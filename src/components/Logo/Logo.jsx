import logo from "../../assets/img/logo.png";

const Logo = () => {
    return (
        <div className="logo">
            <img src={logo} alt="logo Kasa" />
        </div>
    );
};

export default Logo;